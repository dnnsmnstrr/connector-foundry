.venv/
__pycache__/
*.pyc
.pytest_cache/
# Written next to cli/ by `pip install -e .`; regenerated on every install.
*.egg-info/
out/
*.stl
# The only meshes that are sources, not outputs: Mechanism's DeckMate
# models, redistributed under their CC BY-NC 4.0 terms — see README "Licensing".
!parts/deckmate/*.stl
!web/public/**/*.wasm
node_modules/
web/dist/
web/public/scad-bundle.json
web/public/catalogue.yaml
web/public/global-defaults.json
.DS_Store
.claude/worktrees/

# Reference geometry cache: fetched upstreams, converted CAD files, and
# rendered reference meshes. Never committed — some of it is under
# licences this repository cannot redistribute (see references.yaml).
refs/
